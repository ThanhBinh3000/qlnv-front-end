import { ScrollAffixDirective } from './scroll-affix.directive';

describe('ScrollAffixDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollAffixDirective();
    expect(directive).toBeTruthy();
  });
});
